orion.dictionary.addDefinition('name', 'author', {
    type: String,
    label: 'Name'
});

orion.dictionary.addDefinition('email', 'author', {
    type: String,
    label: 'Email'
});

orion.dictionary.addDefinition('image', 'author', {
    type: String,
    label: 'Image'
});

orion.dictionary.addDefinition('website', 'author', {
    type: String,
    label: 'Website URL'
});

orion.dictionary.addDefinition('messagePrimary', 'author', {
    type: String,
    label: 'Primary Message',
    optional: true
});

orion.dictionary.addDefinition('urlPrimary', 'author', {
    type: String,
    label: 'Primary URL',
    optional: true
});

orion.dictionary.addDefinition('messageBreak', 'author', {
    type: String,
    label: 'Message Break',
    optional: true
});

orion.dictionary.addDefinition('messageSecondary', 'author', {
    type: String,
    label: 'Secondary Message',
    optional: true
});

orion.dictionary.addDefinition('urlSecondary', 'author', {
    type: String,
    label: 'Secondary URL',
    optional: true
});